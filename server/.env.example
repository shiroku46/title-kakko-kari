# サーバーのポート番号（Renderでは自動設定されるため不要）
# PORT=3000

# 許可するオリジン（カンマ区切り）。未設定時は全オリジン許可（'*'）。
# 本番環境では Vercel URL と開発用 localhost を列挙することを推奨。
# 例:
# ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:8081,http://localhost:19006
